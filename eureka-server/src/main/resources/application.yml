server:
  port: 3000

spring:
  application:
    name: eureka-server-3000

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka-server-3000:3000/eureka/